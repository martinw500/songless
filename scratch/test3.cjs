const {execSync} = require('child_process'); const res = execSync('yt-dlp --dump-single-json --flat-playlist --no-warnings "ytsearch5:Billie Eilish bad guy audio"').toString(); const entries = JSON.parse(res).entries; console.log('Current duration: 206'); for(let entry of entries) { if (!entry.duration) continue; const diff = 206 - entry.duration; console.log(entry.title, diff); }
